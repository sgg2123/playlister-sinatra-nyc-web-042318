<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1><%= @artist.name %></h1>
      <ul>
        <% @artist.songs.each do |song|%>
        <li><a href='/songs/<%= song.slug %>'><%= song.name %></a></li>
        <li><a href='/genres/<%= song.genres[0].slug %>'><%= song.genres[0].name %></a></li>
        <% end %>
      <ul>
  </body>
</html>
