<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Genres</h1>
      <ul>
        <% @genres.each do |genre|%>
        <li><a href='/genres/<%= genre.slug %>'><%= genre.name %></a></li>
        <% end %>
      <ul>
  </body>
</html>
